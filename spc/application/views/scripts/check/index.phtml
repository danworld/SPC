<h3>Check list:</h3>
<dl>
    <?php foreach ($this->entries as $entry): ?>
    <dt><?php echo $this->escape($entry->password) ?></dt>
    <dd><?php echo $this->escape($entry->hitcount) ?></dd>
    <?php endforeach ?>
</dl>
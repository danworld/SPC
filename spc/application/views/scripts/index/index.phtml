<?php
    $this->form->setAction($this->url());
    echo '<section id="c">'.$this->form.'</section>';
    if ($this->password) {
        echo '<section>The password ('.$this->password.') security factor is '.$this->hitcount.'.</section>';
    }
?>